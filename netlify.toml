[build]
  command = "npm run build"
  publish = "dist"

# SPA: serve index.html for all routes (direct /login, /home, refresh, etc.)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = true
