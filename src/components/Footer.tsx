import { FormattedMessage } from "react-intl";

const Footer = () => {
  return (
    <footer>
      <span>
        <FormattedMessage id="nav.footer" />
      </span>
    </footer>
  );
};

export default Footer;
